const Game = () => {
  return <p>Memory Games</p>;
};

export default Game;

//This page will be for cementing knowledge and possibly learning through games, for example, 
// a match the pairs game so knowledge of words previously sees/learnt is covered again.