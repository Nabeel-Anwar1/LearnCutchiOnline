const Practice = () => {
  return <p>Phrases and words.</p>;
};

export default Practice;

// This section will include words and common phrases, this is where the chunk of the data will be I think with there having to be
// a Cutchi word matching every English word, the reason that I would store this in a database is because I want there 
// to also be a recoding of the words or phrases being spoken by a native speaker so the student can pick up nuances of the language.